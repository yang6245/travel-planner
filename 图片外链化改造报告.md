# 图片外链化改造报告（2026-08-06）

## 问题
弥勒-普者黑云端 trip.json 达 **7.34MB**，超过 GitHub Contents API 的 1MB 读取上限，
导致带 Token 的编辑器读不到数据、报"文件过大"。

## 根因
- 30 张图片以 base64 内嵌在 trip.json 里，占 7.32MB（99.8%）
- 旧压缩逻辑是"单张 >300KB 才压"（压到 1280px/0.82 后仍有 200-400KB/张），总量失控

## 方案（一步到位，图片外链化）
图片全部移出 JSON，存为 GitHub 仓库 `images/MiletoPuzhehei/` 下独立文件，
trip.json 只存 `https://cdn.jsdelivr.net/gh/yang6245/travel-planner@main/images/...` URL。

## 已完成 ✅
| 项目 | 结果 |
|---|---|
| 30 张图片解码重压缩（1280px/0.82，白底去透明） | 上传 images/ 成功 30/30，单张 ≤400KB |
| trip.json 改写为 URL 版 | **7.34MB → 15.6KB（缩小 99.8%）** |
| 云端推送 + 验证 | API size 15,995B（✅ <1MB）；CDN 拉取 15.6KB；0 残留 base64 |
| 图片 CDN 可达 | HTTP 200，image/jpeg，实测 1080×802 正常显示 |
| 两个 HTML 改造 | index.html + MiletoPuzhehei.html：上传即外链（FC 优先+直连回退）、保存前自动外链化残留 base64；语法检查通过 |
| FC 函数升级 v6 | fc_proxy.py 新增 action=upload（白名单开放 images/）；语法检查通过 |
| Token 安全 | 推送完成后 .gh_token.txt 已删除 |

## 待你操作 ⏳（唯一一步）
**重新部署阿里云 FC 函数**（可选，不部署也能用，只是上传图片会走直连慢一点）：
1. 打开阿里云 FC 控制台 → 找到 `github-proxy` 函数
2. 把 `fc_proxy/fc_proxy.py`（v6 版）全部代码粘贴覆盖 → 保存 → 部署
3. 完成后上传图片自动走 FC（国内直连加速）

## 注意事项
- 已登录用户浏览器里的旧缓存（带 base64 大图）**无需手动清理**：下次保存会自动外链化；显示也兼容
- 替换图片会产生旧图残留文件（几 KB~几百 KB），不影响功能，介意可后续清理
- 格聂南线无 base64 图，未受影响；未来任何行程加图都不会再触发 1MB 限制

## 遗留脚本（可复用）
- `migrate_images.py`：把 base64 版 trip.json 迁移为 URL 版（其他行程可复用）
- `push_migration.py`：读取 .gh_token.txt 推送 images/ + trip.json
- `_migrate_out/`：迁移产物本地备份（images/ + URL 版 trip.json）
